# ~/.profile: executed by Bourne-compatible login shells.

if [ "$BASH" ]; then
  if [ -f ~/.bashrc ]; then
    . ~/.bashrc
  fi
fi

mesg n 2> /dev/null || true

# some more ls aliases
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'

